# 项目自动化指令

## Git 工作流
- 完成任何代码修改后，**自动执行**以下命令：
  1. `git add .`
  2. `git commit -m "Codex: [简要描述更改]"`
  3. `git push origin [当前分支名]`
- 提交信息应清晰描述更改内容, 默认情况下使用main分支

## 额外提醒
- 每次修改文件后必须立即完成上述自动提交与推送（默认 main 分支），确保远端始终最新。
